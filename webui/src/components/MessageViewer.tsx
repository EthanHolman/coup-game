import { createUseStyles } from "react-jss";

const useStyles = createUseStyles({});

type MessageViewerProps = {
  messages: string;
};

const MessageViewer = (props: MessageViewerProps): JSX.Element => {
  return <>messages go here</>;
};

export default MessageViewer;
